# Icon
macos-icon = custom-style
macos-icon-ghost-color = #c4a2e0
macos-icon-screen-color = #7ca2d8

# Theme
theme = basalt-dark

# Opacity
background-opacity = 0.95

# Font
font-family = Lilex
font-size = 16

# Shell integration
shell-integration = zsh
shell-integration-features = ssh-env,ssh-terminfo,cursor,sudo,title

# Launch zellij's welcome screen; fall back to a plain shell if it's missing.
command = command -v zellij >/dev/null 2>&1 && exec zellij -l welcome || exec zsh

# Keybindings
# keybind = super+shift+n=new_window
keybind = ctrl+shift+c=copy_to_clipboard
keybind = ctrl+shift+v=paste_from_clipboard
