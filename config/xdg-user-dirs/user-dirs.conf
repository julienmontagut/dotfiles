# xdg-user-dirs-update runs at every login and rewrites user-dirs.dirs: it recreates the
# directories it believes are missing, and reassigns any entry whose directory is absent back to
# the home directory. Either would undo the file next to this one on the next login.
enabled=False
