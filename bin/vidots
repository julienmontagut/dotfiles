#!/bin/bash

# Use XDG_DATA_HOME if set, otherwise use ~/.local/share
XDG_DATA_HOME=${XDG_DATA_HOME:-$HOME/.local/share}

# Use EDITOR if set, otherwise use vi
EDITOR="${EDITOR:-vi}"

# Edit the dotfiles directory
$EDITOR $XDG_DATA_HOME/dotfiles

